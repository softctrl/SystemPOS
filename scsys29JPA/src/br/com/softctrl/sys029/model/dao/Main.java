package br.com.softctrl.sys029.model.dao;


public class Main {

	public static void main(String[] args) {

		// EntityDAO<Coupon> couponDAO = new EntityDAO<Coupon>(Coupon.class);
		// EntityDAO<CouponProduct> couponProductDAO = new
		// EntityDAO<CouponProduct>(
		// CouponProduct.class);
		// EntityDAO<Product> productDAO = new
		// EntityDAO<Product>(Product.class);
		//
		// EntityDAO<User> userDAO = new EntityDAO<User>(User.class);
		//
		// couponDAO.begin();
		// Coupon c = new Coupon(5, 5, null, userDAO.findById(1));
		// Product p = productDAO.findById(10);
		// CouponProduct cp = new CouponProduct(BigDecimal.ZERO, c, p);
		// c.getCouponProducts().add(cp);
		// couponDAO.insert(c);
		// for (CouponProduct t : c.getCouponProducts()) {
		// couponProductDAO.insert(t);
		// }
		// couponDAO.update(c);
		// couponDAO.commit();
		// couponDAO.close();

	}
}
